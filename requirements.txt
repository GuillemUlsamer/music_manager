# Dependencia de sistema (NO se instala con pip, es un binario):
#   FFmpeg + ffprobe, necesarios para convertir el audio a MP3.
#   Windows : winget install --id Gyan.FFmpeg -e

gspread
google-auth
yt-dlp
mutagen
